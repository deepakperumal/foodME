$(document).ready(function() {
  $(document).on('keypress', '.date-format',function(e){
  
  if($(this).val().length>=8)
  e.preventDefault()
      
    if($(this).val().length==2)
    $(this).val($(this).val()+'/')

  });
});
